{% for node in pages %}
  {% if node.title != null %}
    {% if group == null or group == node.group %}
      {% if node.external_redirect != null %}
        <li><a href="{{node.external_redirect}}">{{node.title}}</a></li>
      {% else %}
        <li><a href="{{ BASE_PATH }}/pages/{{node.redirect}}.html">{{node.title}}</a></li>
      {% endif %}
    {% endif %}
  {% endif %}
{% endfor %}
