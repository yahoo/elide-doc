{% if site.url == "http://elide.io" %}
  {% assign BASE_PATH = site.BASE_PATH %}
{% elsif site.url == "http://localhost:4000" %}
  {% assign BASE_PATH = site.url %}
{% else %}
  {% assign BASE_PATH = site.url | append: '/elide-doc' %}
{% endif %}

{% if site.ASSET_PATH != '' %}
  {% assign ASSET_PATH = site.ASSET_PATH %}
{% endif %}
